parsers:
  - name: simpleLTSVExample
    format: ltsv
    time_key: time
    time_format: '[%d/%b/%Y:%H:%M:%S %z]'
    - types:
      line: integer 
      widget: string 
      aKey: string
    # The types part of the structure still needs to be verified
