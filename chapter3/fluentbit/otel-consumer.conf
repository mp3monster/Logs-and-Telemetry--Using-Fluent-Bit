[service]
  flush 1
  log_level debug

[input]
  #We'll use this for Logs
  name opentelemetry
  listen 127.0.0.1
  port 4318

[input]
  # use this for Traces
  name opentelemetry
  listen 127.0.0.1
  raw_traces false
  port 4317

[input]
  # use this for metrics
  name opentelemetry
  listen 127.0.0.1
  port 4316

[output]
  match *
  name stdout